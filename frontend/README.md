# Frontend

This is a simple React application using Vite. It authenticates with Supabase and communicates with the FastAPI backend.

## Setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and fill in your Supabase credentials and backend URL.
